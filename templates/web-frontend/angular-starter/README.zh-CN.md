# Angular 启动模板

本项目由 [Angular CLI](https://github.com/angular/angular-cli) 生成。

## 开发服务器

运行 `npm start` 启动开发服务器。访问 `http://localhost:4200/`。如果更改任何源文件，应用程序将自动重新加载。

## 构建

运行 `npm run build` 构建项目。构建产物将存储在 `dist/` 目录中。

## 运行单元测试

运行 `npm test` 通过 [Karma](https://karma-runner.github.io) 执行单元测试。
