# Svelte + Vite 模板

此模板提供了一个极简的设置，通过 Vite 运行 Svelte，支持 HMR。

## 快速开始

1. 安装依赖：
   ```bash
   npm install
   ```

2. 运行开发服务器：
   ```bash
   npm run dev
   ```

3. 构建生产版本：
   ```bash
   npm run build
   ```
