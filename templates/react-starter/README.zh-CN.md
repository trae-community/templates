# React Starter

[![License: MIT](https://img.shields.io/badge/License-MIT-brightgreen.svg)](https://opensource.org/licenses/MIT)

一个基于 [Vite](https://vitejs.dev/) 构建的现代化、轻量级 React 应用启动模板。它提供了极速的开发环境和优化的生产构建。

[English README](./README.md)

## ✨ 特性

- **Vite 驱动**: 极速的服务启动和 HMR (热模块替换)。
- **React 18**: 支持并发模式等最新 React 特性。
- **极简配置**:合理的默认配置，易于扩展。
- **生产就绪**: 资源优化和代码分割。

## 🚀 快速开始

### 前置要求

- Node.js (推荐 16+ 版本)
- npm 或 yarn 或 pnpm

### 安装

1. 将此模板复制到你的项目文件夹。
2. 安装依赖：

   ```bash
   npm install
   ```

3. 启动开发服务器：

   ```bash
   npm run dev
   ```

4. 在浏览器中打开终端显示的地址（通常是 `http://localhost:5173`）。

## 🛠️ 脚本命令

- `npm run dev`: 启动开发服务器。
- `npm run build`: 构建生产版本。
- `npm run preview`: 本地预览生产构建。

## 📂 项目结构

```text
react-starter/
├── public/           # 静态资源
├── src/
│   ├── App.jsx       # 主组件
│   ├── main.jsx      # 入口文件
│   └── index.css     # 全局样式
├── index.html        # 入口 HTML
├── vite.config.js    # Vite 配置
└── package.json      # 依赖和脚本
```

## 📄 许可证

MIT License.
