# Rust Actix Web 启动模板

使用 Rust 和 Actix Web 构建 Web 服务的启动模板。

## 设置

1. 确保已安装 Rust (rustup)。

2. 运行服务器：
   ```bash
   cargo run
   ```

3. 服务器将在 `http://127.0.0.1:8080` 上运行。

## 端点

- `GET /`: 返回 "Hello World from Rust Actix!"
