# Express.js API 启动模板

一个简单的 Express.js 服务器模板。

## 设置

1. 安装依赖：
   ```bash
   npm install
   ```

2. 启动服务器：
   ```bash
   npm start
   ```

3. 服务器将在 `http://localhost:3000` 上运行。

## 端点

- `GET /`: 返回 "Hello World!"
