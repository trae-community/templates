# Jupyter Notebook 启动模板

用于数据分析的 Jupyter Notebook 启动模板。

## 设置

1. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```

2. 启动 Jupyter Notebook：
   ```bash
   jupyter notebook
   ```

3. 在浏览器界面中打开 `notebook.ipynb`。
