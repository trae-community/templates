# Jupyter Notebook Starter

A starter template for data analysis with Jupyter Notebook.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Start Jupyter Notebook:
   ```bash
   jupyter notebook
   ```

3. Open `notebook.ipynb` in the browser interface.
