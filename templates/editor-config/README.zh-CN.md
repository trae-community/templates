# EditorConfig 模板

[![License: MIT](https://img.shields.io/badge/License-MIT-brightgreen.svg)](https://opensource.org/licenses/MIT)

用于在不同编辑器和 IDE 之间定义和维护一致编码风格的标准 `.editorconfig` 配置文件。

[English README](./README.md)

## ❓ 为什么需要它？

在多人协作的项目中，不同的开发者可能使用不同的编辑器（VS Code, IntelliJ, Sublime Text 等），每个编辑器的默认配置也不尽相同。这会导致：
- 代码缩进混乱（有的用 2 空格，有的用 4 空格，有的用 Tab）。
- 换行符不统一（Windows 使用 CRLF，Linux/macOS 使用 LF）。
- 文件末尾是否保留空行不一致。

使用 `.editorconfig` 可以让编辑器**自动读取并应用**项目指定的格式规则，无需手动配置编辑器设置。

> 🌐 **官方网站**：[https://editorconfig.org](https://editorconfig.org)

## 🚀 快速开始

1. **复制文件**：将本目录下的 `.editorconfig` 文件复制到你的项目**根目录**。
2. **确保生效**：根据你使用的编辑器，确认是否需要安装插件（见下文）。
3. **验证**：打开一个代码文件，查看状态栏的缩进设置是否已自动变为配置文件中指定的规则。

## 🛠️ 编辑器支持

| 编辑器 / IDE | 支持程度 | 所需操作 |
| :--- | :--- | :--- |
| **VS Code** | 需插件 | 安装 [EditorConfig for VS Code](https://marketplace.visualstudio.com/items?itemName=EditorConfig.EditorConfig) |
| **JetBrains IDEs** | 原生支持 | 无需操作 |
| **Visual Studio** | 原生支持 | 无需操作 |
| **Sublime Text** | 需插件 | 安装 [EditorConfig](https://packagecontrol.io/packages/EditorConfig) |
| **Vim / Neovim** | 需插件 | 安装 [editorconfig-vim](https://github.com/editorconfig/editorconfig-vim) |

## 📝 配置详解

本模板包含的通用规则解释：

```ini
# 标记这是最顶层的配置文件，停止向上查找
root = true

# 匹配所有文件
[*]
charset = utf-8                # 统一使用 UTF-8 编码
indent_style = space           # 使用空格缩进（不使用 Tab）
indent_size = 2                # 默认缩进 2 个空格
end_of_line = lf               # 统一使用 LF 换行符（推荐跨平台项目使用）
insert_final_newline = true    # 文件末尾自动插入一个空行
trim_trailing_whitespace = true # 自动删除行尾多余的空格

# 针对 Markdown 文件的特殊规则
[*.md]
insert_final_newline = false   # Markdown 有时需要利用行尾空格换行，所以不强制删除
trim_trailing_whitespace = false

# 针对 Python 文件的特殊规则
[*.py]
indent_size = 4                # Python 官方推荐使用 4 个空格缩进
```

## 📚 完整属性参考手册

| 属性名 | 可选值 | 说明 |
| :--- | :--- | :--- |
| **`indent_style`** | `tab`, `space` | 缩进风格是使用 Tab 还是空格。 |
| **`indent_size`** | 整数, `tab` | 每个缩进级别的列数。 |
| **`end_of_line`** | `lf`, `crlf`, `cr` | 换行符类型。 |
| **`charset`** | `utf-8` 等 | 文件编码格式。推荐始终使用 `utf-8`。 |
| **`trim_trailing_whitespace`** | `true`, `false` | 是否在保存时删除行尾多余的空格。 |
| **`insert_final_newline`** | `true`, `false` | 是否确保文件以一个空行结尾。 |
