# React Native (Expo) 启动模板

使用 Expo 的极简 React Native 应用程序。

## 设置

1. 安装依赖：
   ```bash
   npm install
   ```

2. 启动开发服务器：
   ```bash
   npm start
   ```

3. 使用 Expo Go (Android/iOS) 扫描二维码或按 `w` 在 Web 上运行。
