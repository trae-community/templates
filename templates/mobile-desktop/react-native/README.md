# React Native (Expo) Starter

A minimal React Native app using Expo.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm start
   ```

3. Scan the QR code with Expo Go (Android/iOS) or press `w` for web.
